.next/
node_modules/
.env.local
.env*.local
out/
build/
*.tsbuildinfo
.DS_Store

# Local Claude Code config (may contain tokens)
.claude/

# Raw customer image uploads (staging — processed copies live in public/)
Customer *Images/

# Raw customer upload folders
Customer */

# Local working notes / session handoff (keep private, do not publish)
CLAUDE.md
